{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Expo with Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "exp://localhost:19000",
            "webRoot": "${workspaceFolder}/frontend"
        }
    ]
}
